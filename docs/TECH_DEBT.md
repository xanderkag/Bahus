# Bahus — Технический долг и известные ограничения
# Последнее обновление: 2026-04-20

## 🟡 Статус: ПРОТОТИП

Система находится в фазе проверки гипотезы. До подтверждения коммерческой жизнеспособности
реализация production-grade хранилища, аутентификации и надёжности не планируется.

---

## 🔴 Критические ограничения прототипа (намеренно не исправляются)

### 1. Ephemeral Filesystem (файлы теряются при рестарте Railway)
- **Симптом:** Загруженные файлы хранятся на диске Railway-контейнера. При каждом рестарте или деплое они пропадают.
- **Последствие:** Старые импорты (`queued`) после рестарта не могут быть повторно отправлены в n8n — файл физически удалён.
- **Для прототипа:** Допустимо. Файл загружается → немедленно уходит в n8n → в БД хранятся результаты. До перезапуска всё работает.
- **Решение для продакшн:** Supabase Storage или Railway Volumes (persistent disk).

### 2. Нет аутентификации
- API открыт для любых запросов без токена.
- **Для прототипа:** Допустимо — демо-среда, не публичный URL.
- **Решение для продакшн:** JWT/API-key middleware в `postgres_api.py`.

### 3. Один поток обработки HTTP
- Бэкенд (`ThreadingHTTPServer`) не масштабируется под нагрузку.
- **Для прототипа:** Достаточно для 1-5 одновременных пользователей.

---

## 🔴 ОТКРЫТЫЙ БЛОКЕР (приоритет на 2026-04-20)

### n8n: Финальный тест OpenAI Assistants API

Воркфлоу `bahus_workflow_v2.json` был мигрирован с Extract PDF на OpenAI Assistants API (коммиты `dd922cb` → `b1874d1`).
Последние правки исправили форматирование env var и контекст итерации в Code node.

**Что нужно сделать:**
- [ ] Загрузить `bahus_workflow_v2.json` в n8n UI
- [ ] Проверить env `OPENAI_API_KEY` в n8n
- [ ] Провести тест с реальным «тяжёлым» PDF (векторный прайс-лист)
- [ ] Убедиться, что callback возвращает структурированный JSON в CRM
- [ ] Убрать временные отладочные `alert()` из `src/utils/dom.js` и `src/actions/app-actions.js` после подтверждения

---

## 🟡 Известные баги

| # | Проблема | Файл | Статус |
|---|---|---|---|
| 1 | Двойной импорт при быстром двойном клике | `app-actions.js` | ✅ Исправлен (guard на `status === "saving"`) |
| 2 | Модалка загрузки не сбрасывалась при повторном открытии | `app-actions.js` | ✅ Исправлен |
| 3 | Колонка "Дата" показывала только дату без времени | `app-actions.js`, `overview.js` | ✅ Исправлен |
| 4 | Кнопка удаления импорта: 404 показывала alert | `app-actions.js` | ✅ Исправлен |
| 5 | "Залипание" фильтра при переключении импортов | `app-actions.js` | ✅ Исправлен |
| 6 | Гонка состояний `product_ids` при reload импортов | `app-actions.js` | ✅ Исправлен (`d1c38a9`) |
| 7 | Фоновые поллеры сбрасывали статус `saving` модала | `app-actions.js` | ✅ Исправлен (`6172d7d`) |
| 8 | SyntaxError в вложенных template literals (колонка issues) | `layout.js` | ✅ Исправлен (`d66319f`) |
| 9 | n8n Code node не имеет доступа к `$env` (self-hosted) | `bahus_workflow_v2.json` | ✅ Workaround: нода `Inject API Key` |
| 10 | `Unknown error` в Assistants API Code node при итерации | `bahus_workflow_v2.json` | ⏳ Последний фикс, нужен тест |

---

## 🔵 Технический долг (для продакшн-версии)

| Задача | Приоритет | Описание |
|---|---|---|
| Persistent file storage | Высокий | Supabase Storage вместо локального диска |
| Аутентификация | Высокий | JWT middleware, управление пользователями |
| HTTPS/домен | Средний | Кастомный домен для Vercel (обход РКН) |
| Retry-логика n8n | Средний | Повтор отправки при таймауте n8n |
| OpenAI Assistants cleanup | Средний | Верифицировать, что временные assistant/file удаляются после каждого прогона |
| Unit-тесты API | Низкий | Pytest для критических маршрутов |
| Webhook-подпись | Низкий | HMAC-верификация входящих вебхуков от n8n |
| Убрать debug alerts | Низкий | `alert()` в `dom.js`, `app-actions.js` добавлены для диагностики — убрать перед деплоем |

---

## ✅ Что работает сейчас (статус на 2026-04-20)

- [x] Загрузка файла через UI (drag&drop или выбор)
- [x] Прогресс-бар загрузки с процентами
- [x] Генерация номера импорта `ИМП-YYYYMMDD-XXXX`
- [x] Экран успеха с номером импорта после загрузки
- [x] Автоматический dispatch в n8n после приёма файла
- [x] Polling статуса `GET /api/imports/{id}/status`
- [x] Спиннер на статусе `queued`/`processing` в таблице
- [x] Баннер "Файл передан на обработку" на вкладке позиций
- [x] Удаление импорта с graceful 404 handling
- [x] Метка даты и времени создания импорта
- [x] Структурированные логи n8n-взаимодействия (`[N8N] DISPATCH/RESPONSE/ERROR`)
- [x] Настраиваемые колонки таблиц (show/hide, reorder) с persist в localStorage
- [x] Глобальный поиск по таблице позиций
- [x] Колонки: Поставщик, Актуальность, Артикул производителя
- [x] Настройки колонок мержатся при добавлении новых колонок (не теряют порядок)

## ⏳ Не протестировано / требует проверки

- [ ] Полный цикл с OpenAI Assistants API: загрузка → n8n → callback → статус `parsed`
- [ ] Корректность структурированного JSON из Assistants API в callback
- [ ] Поведение при PDF с нечитаемым текстом (полностью image-based PDF)
- [ ] Поведение при Excel (другой pipeline vs PDF)
