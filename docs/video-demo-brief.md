# Bahus Assistant — Презентация демо-стенда

## 1. Что это и зачем

**Bahus Assistant** — B2B-инструмент для менеджеров, работающих с поставщиками алкоголя.
Закрывает три задачи:

- **Загрузка прайсов** — принимает PDF и Excel от поставщиков, разбирает их через ИИ в структурированную таблицу позиций
- **Ведение номенклатуры** — единая база позиций по всем поставщикам с поиском, фильтрами и нормализацией
- **Формирование КП** — собирает коммерческое предложение под запрос клиента, считает маржу, выгружает в Excel

---

## 2. Текущий демо-стенд

**Стек:** Vercel (фронтенд) + Railway (Python API) + Supabase PostgreSQL + OpenAI

**Что работает прямо сейчас:**
- Загрузка 4 типов документов: Прайс-лист, Нетто-прайс, Промо, Под запрос
- ИИ-разбор PDF через OpenAI Assistants API (GPT-4o + file_search)
- Трёхфазный интерфейс загрузки: форма → отправка → обработка ИИ
- Создание КП: клиент, тема, состав позиций, расчёт маржи
- Экспорт КП в Excel
- Поиск и фильтрация по всей номенклатуре
- Настраиваемые колонки таблицы (drag-to-reorder)

**Статус:** прототип, данные реальные, UI готов к демонстрации.

---

## 3. Инфраструктура и себестоимость (боевой сервер в РФ)

### Почему российский хостинг

При запуске у клиента важно держать всё в России:
- **Данные остаются в РФ** — прайсы, цены, клиентская база не уходят за рубеж
- **Оплата в рублях** — никакой зависимости от Visa/Mastercard и курсовых скачков
- **Надёжность** — нет риска блокировки аккаунта на иностранном сервисе
- **SLA и поддержка** — российский провайдер отвечает по российскому праву

Основной кандидат — **Selectel** (selectel.ru). Также подходят Яндекс Облако и VK Cloud.

---

### Архитектура развёртывания

```
[Nginx / фронтенд]  →  [Python API сервер]  →  [PostgreSQL]
                              ↓
                    фоновый поток (встроен в API)
                              ↓
                     [OpenAI / Azure OpenAI]
                              ↓
                    [Selectel Object Storage]
                    (все загружённые файлы)
```

ИИ-обработка встроена прямо в Python API — отдельный сервис не нужен.
При загрузке документа бэкенд сам анализирует PDF (текст или скан), выбирает режим (text / vision) и отправляет запрос в OpenAI в фоновом потоке.

---

### Конфигурации и стоимость (Selectel, ₽/мес)

#### Вариант A — Стартовый (1–3 пользователя, до 150 документов/мес)

| Компонент | Конфигурация | Стоимость |
|---|---|---|
| Облачный сервер (API + nginx) | 2 vCPU / 4 GB RAM / 80 GB SSD | ~1 800 ₽/мес |
| Managed PostgreSQL | 2 vCPU / 4 GB / 50 GB | ~3 000 ₽/мес |
| Object Storage (файлы прайсов) | 100 GB | ~150 ₽/мес |
| **Итого инфраструктура** | | **~4 950 ₽/мес** |

#### Вариант B — Рабочий (3–10 пользователей, 300–500 документов/мес)

| Компонент | Конфигурация | Стоимость |
|---|---|---|
| Облачный сервер (API + nginx) | 4 vCPU / 8 GB RAM / 100 GB SSD | ~3 500 ₽/мес |
| Managed PostgreSQL | 4 vCPU / 8 GB / 100 GB | ~5 000 ₽/мес |
| Object Storage (файлы прайсов) | 500 GB | ~750 ₽/мес |
| **Итого инфраструктура** | | **~9 250 ₽/мес** |

#### Вариант C — С запасом (10+ пользователей, 1 000+ документов/мес)

| Компонент | Конфигурация | Стоимость |
|---|---|---|
| 2× Облачный сервер (API раздельно для фронта и бэка) | 4 vCPU / 8 GB × 2 | ~7 000 ₽/мес |
| Managed PostgreSQL | 4 vCPU / 16 GB / 200 GB | ~8 000 ₽/мес |
| Object Storage (файлы прайсов) | 2 TB | ~3 000 ₽/мес |
| **Итого инфраструктура** | | **~18 000 ₽/мес** |

---

### Себестоимость разбора документов (ИИ)

Каждый загруженный документ обрабатывается прямо в Python-бэкенде через **OpenAI Chat Completions API (GPT-4o)**. Никакого промежуточного сервиса нет.

Бэкенд сам определяет режим обработки:
- **text** — если PDF содержит извлекаемый текст (обычные прайсы, быстро и дёшево)
- **vision** — если PDF скан или картинка (изображения страниц отправляются в GPT-4o Vision)

В России OpenAI напрямую недоступен — используем **Azure OpenAI** (бизнес-аккаунт Microsoft, легально, оплата в ₽ через партнёров) либо прокси-сервисы типа ProxyAPI.ru.

**Стоимость одного документа:**

| Тип документа | Режим | Токенов вход | Токенов выход | Стоимость |
|---|---|---|---|---|
| Стандартный прайс, 15–25 стр. | text | ~8 000 | ~2 000 | **~3–5 ₽** |
| Большой прайс, 50+ стр., 100+ SKU | text (чанки) | ~30 000 | ~6 000 | **~8–12 ₽** |
| Сканированный PDF, до 10 стр. | vision | ~5 000 + изображения | ~1 500 | **~10–18 ₽** |
| Сканированный PDF, 20+ стр. | vision | тяжело | — | **~25–50 ₽** |

> Скан значительно дороже текстового PDF из-за vision-токенов. Стоит рекомендовать клиентам давать поставщикам шаблоны в Excel/текстовом PDF.

| Объём в месяц | Документов | Себестоимость ИИ | Инфраструктура (вар. B) | **Итого** |
|---|---|---|---|---|
| Малый | 100 | ~400 ₽ | ~9 250 ₽ | **~9 650 ₽/мес** |
| Средний | 500 | ~2 000 ₽ | ~9 250 ₽ | **~11 250 ₽/мес** |
| Высокий | 1 500 | ~6 000 ₽ | ~18 000 ₽ | **~24 000 ₽/мес** |

> **Себестоимость обработки одного документа при среднем объёме ≈ 22 ₽** (инфра + ИИ поровну).
> При масштабировании фиксированная часть (инфра) размывается — цена за документ падает.

---

### Хранение файлов

Selectel Object Storage — S3-совместимое хранилище:
- Все загруженные PDF и Excel хранятся постоянно, не теряются при перезапуске сервера
- Стоимость хранения: **~1,5 ₽/ГБ в месяц**
- Типовой прайс-лист PDF: 1–5 МБ → 1 000 документов = ~3–5 ГБ = **~5–8 ₽/мес**
- Практически бесплатная статья расходов на реальных объёмах

---

## 4. Что нужно доработать (приоритетный список)

### Высокий приоритет — нужно до продакшна

1. **Авторизация пользователей** — сейчас система без логина. Нужен вход по паролю или корпоративному SSO.
2. **Надёжное хранилище файлов** — загруженные прайсы сейчас на диске Railway (эфемерный). При рестарте контейнера файлы теряются. Нужно переехать на Supabase Storage или S3.
3. **Разбор Excel-файлов** — ИИ сейчас умеет разбирать только PDF. Excel (xls, xlsx) нужно либо конвертировать перед отправкой, либо добавить отдельный парсер.

### Средний приоритет — важно для работы

4. **Минимальные партии и кратность** — многие поставщики указывают в прайсе минимум к заказу (6 бутылок, 12 бутылок, 1 ящик). Это поле нужно извлекать и хранить отдельно, иначе при сборке КП менеджер не видит ограничения.
5. **Срок действия прайса** — дата актуальности документа. Нужна автоматическая пометка устаревших прайсов.
6. **Мультипользовательность** — несколько менеджеров с разграничением доступа по клиентам и поставщикам.
7. **Уведомления** — email/Telegram при завершении разбора документа.

### Нюансы прайс-листов, которые надо учесть

- **Минимальная покупка**: одни поставщики ставят минимум 6 бут., другие 12, третьи — целый ящик (6/12/24). Это влияет на КП — нельзя поставить qty=1, если минимум 6.
- **Кратность**: заказ кратный 6 или 12. Если клиент хочет 10 бутылок — нужно округлять до 12.
- **Условия НДС**: прайс с НДС или без — влияет на расчёт маржи. Сейчас флаг есть на уровне поставщика, но не на уровне отдельной строки.
- **Промо-условия**: часть прайсов содержит временные акции с датой окончания — без этого поля менеджер не знает, актуальна ли цена.
- **Валюта строки**: некоторые поставщики указывают цены в USD или EUR с пересчётом по курсу ЦБ — нужен отдельный курсовой механизм.

---

## 5. Что показываем в видео (сценарий)

1. **Открываем "Импорт файлов"** → кнопка "Загрузить" → модалка с формой
2. **Загружаем 4 документа по очереди**, меняя тип:
   - `Прайс-лист` — общий прайс поставщика
   - `Нетто-прайс` — закупочные цены для расчёта маржи
   - `Промо` — акционные позиции с ограниченным сроком
   - `Под запрос` — бланк под конкретного клиента
3. **Наблюдаем обработку** — 3 фазы: отправка → ИИ разбирает → результат (N позиций)
4. **Переходим в "КП"** → "Создать новое КП" → выбираем клиента, вводим тему запроса
5. **Показываем сформированное КП** — состав позиций, маржа, итог по сумме
6. **Экспортируем в Excel** — файл готов к отправке клиенту

---

## 6. Ключевой месседж для клиента

> Менеджер тратит 15–30 минут на ручной ввод одного прайса. Bahus делает это за 2–3 минуты включая ИИ-разбор. При 50 прайсах в месяц — это освобождение 12–25 часов работы менеджера ежемесячно.
